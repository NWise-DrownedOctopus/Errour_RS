pub struct Creature {
    pub position_index: usize,
    pub collider_index: usize,
    pub animation_index: usize,
    pub sprite_sheet_index: usize,
    pub dead_flag_index: usize,
    pub health_index: usize,
    pub drop_table_id_index: usize,
}